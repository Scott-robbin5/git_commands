# h1
## h2
### h3

 *em*

 _also em_

 **strong**

  __also strong__

* this
* is
* an
* unordered
* list
- another
- one
+ and
+ another
+ one

1. one
2. two



* clone -clone an existing repo
* checkout -switch branch
  * -b -create a branch
* status -cheak what you added
* push -push current branch
  * -u - push a branch you never have
* pull - get changes and merge them with your current
* add -Adds an untracked file
* commit -makes an open text editor
  * -m -makes a message
  * -a -commit all unstaged changes
  * -am -commit unstaged changes w/ messages
* branch -shows brankes
  * -a -all branches of the repo
  * -d - deleat a branch
* merge -merges two seprate branches